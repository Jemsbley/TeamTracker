# Where the frontend should send API calls.
# Local dev (with backend running via `npm run dev` in server/):
VITE_API_URL=http://localhost:4000
# Production example:
# VITE_API_URL=https://teamtracker-api.up.railway.app
